1. a)число -> список цифр 238 -> [2, 3, 8]
   б)сумма цифр 2 3 8 -> 13
начать с тестов, написать функции так, чтобы программа запустилась

2. settings больше не синглетон, он передаётся в конструкторы классов, в которых нужен
писать не в файл, а в String (используем BufferedString вместо PrintStream)

тесты(начать с тестов!)
результат содержит определённые теги
                <svg
                <rect
